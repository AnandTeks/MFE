import { Routes } from '@angular/router';
import { CarSearchComponent } from './car-search/car-search.component';

export const CAR_SEARCH: Routes = [
    {
        path: 'car-search',
        component: CarSearchComponent
    }
];